import { CityValidateDirective } from './city-validate.directive';

describe('CityValidateDirective', () => {
  it('should create an instance', () => {
    const directive = new CityValidateDirective();
    expect(directive).toBeTruthy();
  });
});
